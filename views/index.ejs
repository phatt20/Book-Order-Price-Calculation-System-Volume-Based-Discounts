<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Harry Potter Bookstore</title>
    <link rel="stylesheet" href="styles.css"> <!-- เชื่อม CSS ที่คุณสร้าง -->
</head>
<body>
    <h1>“บ้านนายดิน”</h1>
    
    <form action="/calculate" method="POST">
        <h2>เลือกหนังสือที่ต้องการซื้อ:</h2>
        <div class="book-container">
            <% books.forEach((book, index) => { %>
                <div class="book-item">
                    <img src= "/img/image<%= index + 1 %>.jpg" alt="<%= book %>" class="book-image">
                    <h3><%= book %></h3>
                    <label for="num">จำนวนหนังสือ</label>
                    <input type="number" id="num" name="quantities" placeholder="0" min="0">
                </div>
            <% }) %>
        </div>

        <button type="submit">คำนวณราคา</button>
    </form>
    
    <hr>

    <h3>ผลลัพธ์:</h3>
    <p>ราคาทั้งหมด: <%= totalPrice %> บาท</p>
    <p>ส่วนลด: <%= discount %> บาท</p>
    <p>ราคาสุทธิ: <%= finalPrice %> บาท</p>

    <% if (selectedBooks.length > 0) { %>
        <h4>หนังสือที่เลือก:</h4>
        <ul>
            <% selectedBooks.forEach(book => { %>
                <li><%= book %></li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>ยังไม่ได้เลือกหนังสือ</p>
    <% } %>
</body>
</html>
